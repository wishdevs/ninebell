import type { NextConfig } from 'next';

// 디자인 기본형 템플릿 — 백엔드/배포 설정 없이 정적 프론트만 다룬다.
// Next 16은 `next build` 시 ESLint를 실행하지 않으므로 별도 설정이 필요 없다.
const nextConfig: NextConfig = {};

export default nextConfig;
