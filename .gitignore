# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env.*
!.env.example

# archives(비밀 유출 방지 — backend/.env.zip 등)
*.zip

# typescript
*.tsbuildinfo
next-env.d.ts

# local recycle bin
.recycles/

# python backend
backend/.venv/
backend/venv/
**/__pycache__/
*.py[cod]
backend/.env
backend/.env.zip
backend/.pytest_cache/
.ruff_cache/

# Claude Code 에이전트 워크트리(전체 리포 사본) — Tailwind v4 가 gitignore 밖 파일을
# 전부 스캔하므로 여기 없으면 워크트리 내 문서 문자열이 빌드를 깨뜨릴 수 있다(2026-07-05 실측).
.claude/worktrees/
deploy/api/app.env
deploy/api/.env
deploy/db/.env
deploy/front/.env
