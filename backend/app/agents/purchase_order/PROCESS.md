# 구매발주 — 프로세스 정의 (purchase-order)

> SCM-구매 계열 첫 에이전트. 결의서(GLDDOC00300) 패밀리가 아니라 **구매 3화면 파이프라인**이다:
> ① 프로젝트BOM구매요청[나인벨](PUOPRQ00200) → ② 구매요청처리[나인벨] → ③ 구매발주일괄입력[나인벨].
> 상태: **v0.3(2026-08-28) — Phase B 쓰기 개방**: D4 이동요청 저장(`nodes/save_move`) · D5 발주단위
> 저장 루프(`nodes/save_units`) · D7 셀프결재 상신(`nodes/self_approve`, 화면 ② `PURCHASE_REQ_PROCESS`)
> 을 그래프에 연결했다(프리미티브는 `steps_write.py`). 게이트 = 계획 확정 → **confirm HITL(저장)** →
> 저장 → **confirm HITL(상신)** → 상신(빌더 allow_submit ∧ ¬params.debug). 헤디드 관찰은 env
> `ERP_HEADLESS=0`(config.erp_headless). 사용자 지시: ETRI-001 을 헤디드로 지켜보며 첫 실측.
> ⚠ 첫 실행에서 확정할 것: 저장이 `checkRow` 체크를 반영하는가 / 결재 아이콘 2종 중 어느 쪽인가 /
> 상신 후 회수 경로(eap-approval-cancel). 아래 본문은 v0.2 실측 기록 그대로다.
> 종전 상태: **v0.2 — 화면 ①②③ 전 구간 사용자 시연(2026-08-25 스크린샷 8+1장)으로 경로 확정.
> 화면 ① 읽기는 라이브 프로브 ✅(2026-08-13). 쓰기(저장)·상신·화면 ②③ 조작은 경로만 확정이고
> 헤드리스 실측은 미완.** 시연 근거 사실은 `✅(시연)`, 프로브 실측은 `✅`, 미확정은 `❓`로 구분한다.
> 화면 흐름 확정본(프론트): `src/components/live/simulation/purchase-order/` 공용 조각
> (`buildPlanPayload` 가 실행 파라미터 계약 초안). 흐름을 확정했던 데모 에이전트
> `purchase-order-demo` 는 2026-08-21 제거 — 조각은 LivePlannerCard(계획서 HITL)가 계속 쓴다.

## 개요

사용자가 **계획서(실행 전 플래너)** 에서 실 프로젝트를 검색해 BOM 을 불러오고, 3레벨 모듈을
발주단위로 묶어 발주단위별 구매사유·납기예정일과 거래처 그룹별 실거래처·납기·비고를 확정한다.
에이전트는 그 계획서를 받아 ERP 반복 루프(발주단위마다 조회→선택→입력→저장)를 대신 돈다.
반복 입력을 그때그때 받지 않고 최초 1회 계획서로 일괄 수령하는 것이 설계의 핵심.

## 확정된 업무 결정(사용자, 2026-08-12 ~ 08-25)

- **D1 입력 방식**: 프로젝트는 **실행 전 폼 필수**, 계획서만 **실행 중 HITL**(2026-08-14
  갱신 — 프로젝트 선택 개입·폴백 제거, 사용자 지시: 폼에서 이미 골랐는데 실패했다고 다시
  고르라는 개입은 이중 입력). 실행 시작 → 로그인/SCM-구매/화면 진입 → **프로젝트 적용**
  (실행 전 선택분, 실패 = 하드 실패) → BOM 조회 → **계획서 개입**(전체폭 — 구 데모에서
  확정한 화면 흐름의 공용 조각 재사용: 발주단위 묶음 + 구매사유·납기 + 거래처 지정)
  → 발주단위 반복 실행. 실행 전 REST 조회 방식은 기각(세션 관리 신설·조회 대기 UX 부담).
- **D2 사용자유형**: 로그인 후 **SCM-구매** 선택(2026-08-13 사용자 지정). 검증: ✅ 2026-08-13
  프로브 — `.user-info` 패널 라벨 정확히 'SCM-구매'(옵션: 회계사용자(예외)/인사사용자(예외)/SCM-구매),
  이트라이브2 계정 보유, 기존 `ensure_user_type(page, "SCM")` 이 **무수정** 통과
  (`menu_schemas.USER_TYPE_SCM` 별칭 기등록).
- **D3 화면 ① 고정값**: 구매그룹=**나인벨**, 구매조직=**나인벨** — 검증: ✅ 진입 시 자동
  기본값(코드 1000), 통화 KRW.
  ⚠ **구매사유 정정(2026-08-25 시연)**: "비운다"는 **진입 직후 프리필 제거**만을 뜻한다.
  D5 반복 루프에서는 같은 필드에 **발주단위별 구매사유를 채워 넣고** 저장한다(시연 실측값
  `ZJ90-130, 8CH BS PROCESS BUFFER MODULE` = `finalPurchaseReasonOf` 산출물과 동형).
  현재 `ensure_fixed_header` 는 무조건 비우기만 하므로 루프용 **세터 경로 추가 필요**.
  사용자는 이 입력을 "상단 필터행"이라 부른다. ✅ **확정(2026-08-26 드라이런)**: `#i_rmk_dc` 는
  상단 폼 텍스트 필드이고 **`[적용]` 버튼이 없다** — 값만 넣고 저장하면 된다.
  **강제 구현(2026-08-14, 사용자 지시 '선택이 안 되어 있으면 선택')**: `steps.ensure_fixed_header`
  가 프로젝트 적용 직후·조회(F2) 직전에 확인하고 어긋났을 때만 보정한다. 마크업 실측 —
  코드피커는 **코드(hidden `#i_purgrp_cd`/`#i_purorg_cd`) + 표시(`#…_text`)** 쌍이고
  구매사유는 평범한 텍스트박스 `#i_rmk_dc`. ⚠ **코드만 세팅하면 표시가 해석되지 않는다**
  (프로브 (a) 실패) → **코드+표시 동시 세팅**이 검증된 경로이며((b) 성공, 복구 후 조회 257행
  정상), 세팅 후 화면을 다시 읽어 독립 확인한다. 정상 경로(3지점 실측: 진입·적용 후·조회 후
  모두 1000/나인벨)에선 아무것도 바꾸지 않는다.
  ⚠ **Phase B 선행조건**: 이 보정은 폼 값(제출 code + 표시)만 맞춘다 — 코드피커 **위젯 내부
  모델** 동기화는 저장 경로가 없어 미검증이다. 저장 게이트를 열기 전 '보정 후 저장' 실측 필수.
  딥링크 3종 확정(✅ `navigate_menu()` 무수정 진입):
  ① `/PU/PUOPRQ00200_X20616` ② `/PU/PUOPRQ00300_X20616` ③ `/PU/PUOORD02000_X20616`
  (모듈코드 PU + 나인벨 커스텀 접미 `_X20616`).
- **D4 이동요청 선행(화면 ①, 1회)**: 진입 시 두 체크박스는 **둘 다 체크 상태**(무필터·합집합).
  "이동요청만" = **구매요청 해제**. 체크박스 셀렉터 ✅ — 구매요청 `#s_pu_chk`, 이동요청
  `#s_move_chk`(**`label[for]` 직결로만** — 두 체크박스가 같은 `<span>` 에 나란해 li 스코프
  탐색은 항상 첫 번째만 잡는 버그, 프로브 실측). 필터 의미 ✅ — 구매요청 체크=`MV_FG='N'`
  리프, 이동요청 체크=`MV_FG='Y'` 리프(+구조행), CX85-137 실측 410/354/73행 수치 정합.
  이후: 조회 → **전체 선택** → 이동출고저장위치=**공용자재**, 이동입고저장위치=**프로젝트** →
  저장. ⚠ 비가역(저장 게이트 뒤).
  **두 저장위치는 항상 이 값 고정**(사용자 확정 2026-08-25) — 계획서로 받지 않고 코드에 박는다.
  필터행 6종은 각각 `[적용]` 버튼을 갖는다. **저장위치는 코드피커이고 필드·버튼 id 와
  반영 컬럼까지 실측 확정** — '화면 ① 쓰기 경로 실측' 절 참조(2026-08-26).
  ✅(시연) 저장 성공 신호 = 하단 토스트 **"자료가 정상적으로 저장되었습니다."** +
  상단 **이동요청번호** 자동 발급(`IRQ2026070961` 형태).
  ⚠ **실행 순서 변경(사용자 확정 2026-08-25)**: 이동요청 저장은 **계획서 승인 뒤**로 옮긴다
  (BOM 읽기 → 계획서 HITL 승인 → 이동요청 저장 → 발주단위 저장). 비가역 저장이 전부 사용자
  승인 뒤로 가므로 기존 설계보다 안전하다.
- **D5 반복 루프 = 발주단위(화면 ①)**: 이동요청 해제(구매요청만) → 조회 → 계획서의 발주단위
  순서대로: SET 레벨 모듈 선택(1개 이상) → 납기예정일 + 구매사유 입력 → 저장(= 구매요청 저장
  1회 = 구매요청번호 1건, `PRQ2026070639` 형태). ⚠ 비가역.
  **다이얼로그 2종**:
  | 시점 | 문구 | 처리 | 상태 |
  | --- | --- | --- | --- |
  | **재조회** | `저장하지 않은 데이터가 있습니다. 조회를 계속하시겠습니까?` | **[예]** + 조회 2회 | ✅ 확정(2026-08-26) |
  | **저장** | `저장하시겠습니까?` | **[예]** | ✅(시연) |
  ✅ **초기화 다이얼로그 해소(2026-08-26 드라이런)**: 조건은 **미저장 변경 존재**다. 2026-08-25
  프로브가 4/4 무발생이었던 것은 `[적용]` 이 실패해 그리드에 변경이 없었기 때문이고, 저장위치가
  실제로 꽂히자마자 떴다. 문구·버튼·재시도 규율은 '화면 ① 쓰기 경로 실측' 절 참조.
  **입력 위치 ✅(시연)**: 납기예정일 = **필터행 `납기예정일` + `[적용]`**(선택 행 일괄 적용),
  구매사유 = 상단 `구매사유` 입력(D3 참조).
  **행 선택 ✅(시연)**: 레벨3 SET 행을 체크하면 **하위 레벨4 부품이 전부 자동 체크**된다
  (`checkChildren()` 동작과 부합). 레벨1(프로젝트)·레벨2(장비) 행은 체크하지 않는다.
  선택 **세터 확정(2026-08-26 드라이런)**: **`grid.checkRow(<ds행>, true)`** — 자손 자동 전파.
  ⛔ `checkItem`/`getCheckedItems` 는 **아이템 인덱스 공간**이라 ds 행 번호와 섞으면 안 된다
  ('화면 ① 쓰기 경로 실측' 절의 인덱스 공간 함정 2).
- **D5-보충 BOM 레벨 재정정(levelmap 프로브 2026-08-13)**: 트리그리드는 **5레벨**
  (`ds.getLevel()` 0-idx: **0=루트/1=프로젝트(라벨 행)/2=장비/3=SET/4=부품**). 사용자 용어
  '3레벨'(발주단위 선택 대상)은 **getLevel()==3 인 SET 행 15개**('외주조립-*' 등)에 대응.
  구매요청만 필터 = **프로젝트 1 + 장비 1 + SET 15 + 리프 337 = 354행**(xlsx 와 일치.
  루트는 count 밖 — ds 인덱스 0 의 숨은 루트, 데이터 행은 1..count. 한때 '리프 336' 으로
  기록했던 것은 0..count-1 루프가 마지막 행을 놓친 경계 아티팩트 — boundary 프로브로 정정).
  CX85-137 리프 392(무필터)·ZJ90-130(PJT_NO 2261) 리프 55 — 두 수치는 0-베이스 루프 실측이라
  경계 아티팩트 ±1 가능(재실측 시 393/56 예상, 게이트에 미사용). ⚠ 필드 읽기는 반드시
  `ds.getValue` — `grid.getValue` 는 한 행 앞섬(아래 'BOM 트리그리드 실측' 인덱스 공간 함정).
- **D5-보충2 모듈은 하위 부품이 있는 SET 만이다(사용자 확정 2026-08-14)**: 구매요청만 필터를
  적용하면 **4레벨 부품이 한 행도 없는** 프로젝트가 있다 — ZJ90-130 은 17행 = 프로젝트 1 +
  장비 1 + **SET 15**(외주조립-*)뿐이고 리프가 0이다. 이는 **그 SET 들의 발주가 이미 끝났다**는
  뜻이며(리프가 남아 있어야 발주 대상), 계획서에 띄우면 안 된다. 규칙: **parts 가 빈 module 은
  버리고**, 그 결과 module 이 0인 machine 도 버린다(`planner.assemble_planner_bom`). 남은
  모듈이 0이면 `read_bom` 이 `no_modules` 로 **조기 종료**한다(END — 빈 계획서 HITL 금지).
  ⚠ 2026-08-13 에 같은 화면을 보고 넣었던 '자식 없는 SET = 그 행 자체가 구매 대상' 보정은
  완료분을 발주 대상으로 노출하던 오독이라 철회했다.
  ✅ **이 해석 확증(2026-08-25)**: 사용자 시연이 바로 ZJ90-130(2261)로 **2026-07-24 에 발주를
  완료**한 기록이었다(`PRQ2026070639`). 8/13 프로브가 본 '리프 0' 은 발주 완료 상태였던 것이며,
  `no_modules` 조기 종료 동작이 옳다. 후속 테스트 대상은 **2285 `GY03-019, 12CH-L3`** 로 교체.
  ✅ **2285 실측(2026-08-25 프로브, 검색어 `GY03-019`)** — 발주 대상 있음, 쓰기 테스트에 즉시 사용 가능:
  | 필터 | 행수 | 구성 | PUR_FG | MV_FG |
  | --- | --- | --- | --- | --- |
  | 둘 다 체크 | 797 | — | Y 766 / N 31 | Y 133 |
  | **구매요청만** | **664** | 프로젝트 1 + 장비 2 + **SET 28** + **리프 633** | Y 633 / N 31 | 전부 N |
  | 이동요청만 | 164 | 구조행 31 + 리프 133 | Y 133 / N 31 | 전부 Y |
  **SET 28개 전부 리프 보유** → D5-보충2 필터로 버려지는 SET 0건. `WBS_NM` = `PO-2026-07-1457`.
  최대 `GY03-019-PROCESS-013 전장-제조1팀`(64리프) / 최소 `외주조립-명판`(1리프).
  CX85-137(2297, 리프 337)과 함께 **정상 프로젝트 대조군 2종**으로 쓴다.
- **D6 구매사유·비고 조립(2026-08-14 사용자 확정)** — **입력란은 조각만 받고, 완성은 최종
  계획서에서** 한다. 입력값에 접두를 섞어 넣으면 사용자가 필드를 한 번 수정하는 순간 그 시점의
  프로젝트·구매사유가 오버라이드에 굳어버린다(그래서 이 구조로 바꿨다).
  | 필드 | 입력란이 받는 것 | ERP 로 나가는 최종값 |
  | --- | --- | --- |
  | 구매사유 | 외주조립 **모듈명만**(`BUFFER·ELECTRIC PANNEL`, 복수 `·` 연결, 없으면 빈 값) | `[프로젝트코드 · 프로젝트명] [모듈명]` |
  | 비고 | **메시지만** — `가공품 거래처(해룡) 직배송`. 빈 값이 되는 경우 둘: 가공품 그룹 자신 / **발주단위에 가공품이 없을 때**(직배송 대상이 없으므로 미기입, 2026-08-14) | `[최종 구매사유] [메시지]` — 메시지는 대괄호로 감싸고, 없으면 대괄호도 없음 |
  최종값은 `model.ts` 의 `finalPurchaseReasonOf`·`finalNoteOf` 가 만들며 **최종 계획서 표기와
  제출 페이로드가 같은 함수를 공유**한다(보이는 것 = 전달되는 것). 비고의 앞부분은 구매사유
  필드와 **같은 완성 문자열**이라 두 필드가 어긋나지 않는다.
- **D7 셀프 결재(화면 ②) — 상신 예외 승인됨(2026-08-25)**: "셀프결재"는 버튼 이름이 아니라
  **결재라인에 본인만 넣고 상신**하는 것을 뜻한다(사용자 정의). 즉 **전자결재 상신**이다.
  - 조회: 공장=**나인벨**(필수), 구매요청일 범위(기본 오늘~오늘), 요청번호. ①이 발급한
    **`PRQ…` 번호로 대상을 특정**하는 것이 확정 경로다.
  - ✅(시연) 마스터 컬럼: 요청번호·구매요청일·요청유형(원재료)·구매유형(표준)·구매그룹·요청자·
    **결재상태**·**결재상신코드**·비고·첨부. 하위 탭 4종(품목/거래처/계정/발주).
  - ✅(시연) **결재상태 3값 = `저장` → `진행` → `종결`**. 상신 전은 `저장` + 결재상신코드 **빈칸**,
    상신 후 `(주)나인벨-2026-14077` 형태 코드가 채워진다.
    → **가드레일**: 상신 대상은 `결재상태=='저장' && 결재상신코드==''` 인 행으로 한정한다.
  - ✅(시연) **결재 진입 = 우측 상단 아이콘(툴팁 "결재")**. 툴바 버튼(요청취소·요청마감·마감취소·
    첨부파일)과는 별개다. 아이콘 셀렉터 — 검증: ❓.
  - ✅(시연) 결재창은 **회계전표 계열과 동일한 EAP 팝업**
    (`uc.ninebell.co.kr/#/popup?...MicroModuleCode=eap`, 버튼 `미리보기 / 보관 / 상신`).
    결재란에 담당 1명(본인)만 표시 = 셀프. 제목 자동 = `PRQ…-구매팀-<기안자>`.
    ⛔ **보관 버튼은 클릭 금지**(리포 공통 규칙).
  - **승인 조건(사용자 지시 2026-08-25)**: 상신 자동화는 승인되었으나 **프로브·중간 테스트에서는
    상신하지 않는다.** 그 앞 구간(①저장 · ②조회/선택/결재창 진입)이 전부 통과한 뒤 **맨 마지막에**
    상신을 켠다. 코드에서는 voucher 계열 `allow_submit` 과 같은 **별도 플래그 게이트**로 잠근다.
  - **회수 경로 — 검증: ❓(선행 필수)**: EAP 창이 같으므로 기존 `eap-approval-cancel`
    (결재취소→상신취소→삭제)이 이 문서도 회수할 가능성이 높다. 화면 ②의 `요청취소` 가 상신 후에도
    동작하는지와 함께 **상신을 켜기 전에 실측**한다. 회수 불가로 판명되면 상신 직전에서 멈추고
    마지막 클릭은 사용자에게 넘긴다.
- **D8 화면 ③ 거래처 적용 — ✅(시연) 전 구간 확정**:
  - 랜딩에서 자동 채움: 공장·구매그룹·구매조직=**나인벨**, 통화 KRW/환율 1, 부가세포함여부=
    부가세별도, 세무구분=과세매입, 구매발주일=오늘. **사람이 넣는 건 `구매발주유형`(필수, 빈칸)**
    = **원재료**.
  - 우측 상단 **`구매요청` 버튼** → 모달 **`구매요청`** 팝업.
  - 팝업 조회조건: 요청유형=**원재료**(필수) / 구매유형 / 요청일 범위 / 공정외주여부(No, 비활성) /
    품목 / 저장위치 / 품목 주거래처 / **변경 거래처 + `[적용]`** / 구매요청번호 /
    **`WBS 요소`(드롭다운+돋보기)**.
    ⚠ **정정**: `WBS 요소` 의 값은 **프로젝트명**(`ZJ90-130, 8CH BS P…`)이다 — 계획서의
    `wbs`(`PO-2026-06-8668`)가 아니라 **`project.name`** 으로 고른다. `PO-…` 는 본화면 마스터의
    `WBS` 컬럼 / 팝업 하단 디테일의 `WBS요소` 컬럼 값이다.
  - 팝업 마스터 컬럼: 프로젝트 · 요청자 · 구매요청번호 · 요청일 · 품목 · 품목명 · 규격 ·
    구매요청거래처 · 요청수량 · **품목거래처명** · **변경거래처명** · **변경거래처**.
    팝업 하단 디테일: 계정수량 · WBS요소 · 프로젝트 · 설비오더번호 · 예산단위 · 예산계정 · 계정 ·
    비용센터(=구매팀) · 자산 · 수주번호.
  - 절차: **`품목거래처명` 컬럼 정렬** → 같은 값 그룹(가공품/판금품/…) 행 체크 →
    상단 `변경 거래처` 지정 → **`[적용]`** → 반복 → 전체 선택 → 하단 **`적용`** 으로 본화면 반영.
    (자동화에서는 정렬 없이 **`품목거래처명` 값이 일치하는 행만 체크**하면 동일 결과. 화면 표시용
    으로만 정렬한다 — 사용자 확정 2026-08-25.)
  - ✅ **성공 판정 신호**: `[적용]` 직후 해당 행의 **`변경거래처명`·`변경거래처`(코드) 컬럼이
    채워진다.** 실측 코드값 — 주식회사 해룡엔지니어링 `30209`, 알파테크 `10061`.
  - **거래처 값의 출처 = 계획서**(사용자 확정 2026-08-25). 종전 문서의 '기본 해룡/알파테크' 표기는
    폐기 — 발주 패턴(`settings.order_patterns`)과 계획서 오버라이드가 정한 `vendorGroups[].vendor`
    를 그대로 넣는다.
  - ⚠ 하단 `적용` 은 본화면에 발주 초안을 만든다(저장 전). 비가역 여부 — 검증: ❓.
- **D9 거래처 그룹별 마감(화면 ③)**: 거래처별 그룹 행을 하나씩 열어 하위 전체 선택 →
  납기예정일 입력, 비고 수정 가능. 납기 파생 규칙(계획서와 동일): 가공품=발주 납기,
  그 외=발주 납기의 **영업일 5일 전**(1주=영업일 5일, 공휴일·주말을 건너뛰며 보존 —
  2026-08-21 사용자 확정으로 '달력 7일−공휴일 보정' 방식 대체. 프론트 `dates.ts`
  `subtractBusinessDays` 와 동일 알고리즘 — 백엔드 이관 시 단일소스화).
  비고 기본 = `가공품 거래처(<가공품 거래처명>) 직배송`.
  **✅(시연) 본화면 구조**:
  - **마스터 1행 = 거래처 1건.** 컬럼: 발주번호(저장 전 빈칸) · 구매발주일 · **WBS**(`PO-2026-06-8668`) ·
    프로젝트 · **거래처명** · 발주유형 · **결제조건** · 통화 · 환율 · **비고**.
    `결제조건`은 거래처별로 자동 부여된다(`익월 말일 결제` / `익익월 말일 결제` 실측).
  - **비고는 마스터 행의 인라인 편집**이며, 진입 시 이미 `<프로젝트명> <모듈명>`
    (`ZJ90-130, 8CH BS PROCESS BUFFER MODULE`)로 채워져 있다. 여기에 메시지를 덧붙이는 형태가
    **D6 규칙 `[최종 구매사유] [메시지]` 와 정확히 일치**한다 → 계획서 `vendorGroups[].note`
    를 그대로 쓰면 된다.
  - **납기 입력 UI**: 디테일 그리드 **위**의 `납기예정일 [날짜] [적용]`. 디테일 행에는 구매요청에서
    넘어온 납기(`2026-10-19`)가 이미 들어 있고 `[적용]`이 선택 행을 덮어쓴다.
  - 디테일 컬럼: 프로젝트 · **WBS 번호**(`2261-01`) · 품목 · 품목명 · 규격 · 단위 · 수량 · 단가 ·
    금액 · 장부단가 · 장부금액 · 부가세 · 세무구분 · **납기예정일** · 비고.
  - **종료 = 툴바 💾 저장**(사용자 확정 2026-08-25). 이후 별도 결재 단계 없음. ⚠ 비가역.
  - 납기 기준(사용자 확정 2026-08-25): **발주단위 납기가 상위 기준이고, 거래처별 납기는 그 안의
    세부값**이다 — 계획서의 `units[].dueDate` / `vendorGroups[].dueDate` 2층 구조와 그대로 대응.
- **D10 프로젝트 검색**: 검증: ✅(부분) 2026-08-13 프로브 — **"프로젝트 도움창" 코드피커**
  (자동완성 아님). 오픈은 라벨 '프로젝트' 우측 돋보기(기존 `js_lib.PROJECT_PICKER_BOX_JS`
  무수정 동작), 검색어 입력 `#keyword`, 결과는 플랫 `.dews-ui-grid`. 필드 매핑은 WBS 스키마가
  아니라 **`PJT_NO`(코드)/`PJT_NM`(명)** + `START_DT`/`END_DT`/`RSPNBER_EMP_NM`(담당)/
  `PJT_ST_NM`(상태). 'CX85-137' 검색 = 정확히 1건(코드 2297) 확인. 백엔드 검색 계약 초안:
  `{"keyword":"CX85-137"}` → `[{"code":"2297","name":"CX85-137, 12CH PROCESS","start_date":…,
  "end_date":…,"manager":…,"status":"승인"}]`. **적용 사이클 ✅**(2026-08-13 후속 프로브,
  CX85-137·ZJ90-130 2/2): 검색(팝업당 **1회만**) → 행 선택(`setCurrent`+`setSelection`,
  fieldName='PJT_NM') → 적용 클릭 → 팝업 닫힘 → 메인 필드 반영(`FIELD_DISPLAY_JS`) →
  조회(F2) → BOM 로드. ZJ90-130 = PJT_NO **2261**(실재 확인).
  ⚠ **팝업 소멸 근본 원인 확정(2026-08-14 라이브 프로브 4/4 — 종전 '간헐 소멸' 정정)**:
  `#keyword` 는 action/method/onsubmit 없는 `<form>` 안이라 **trusted Enter**
  (`page.keyboard.press`)가 네이티브 폼 제출을 유발하고, SPA 가 이를 앱 소프트리셋
  (MainContainer 재마운트 — framenavigated 0건·URL 불변)으로 처리해 팝업이 **영구** 소멸한다.
  대기시간·그리드 준비 여부와 무관(결정적). 검색 제출은 **untrusted KeyboardEvent 디스패치**
  (`js.SUBMIT_KEYWORD_JS`)로만 한다. 추가 실측: 팝업은 열리면서 메인폼 현재 프로젝트값으로
  **자동 사전검색**돼 1행이 이미 떠 있고(빈 목록 아님), 창 셸 출현(클릭 후 79~187ms)과
  `#keyword`/그리드 준비(345~449ms)가 분리돼 있다 → 열림 판정은 `#keyword` 를 품은 창 +
  그리드 dewsControl 준비(`js.POPUP_STATE_JS`), 검색 수락은 제출 전 시그니처(rowCount·첫행
  PJT_NO) 변화 1차 판정. **팝업당 검색 1회 + 재오픈 재시도(상한 2회)** 완화책은 유지.
  ⚠ **갈림길은 제출이 아니라 '주입'이다(매트릭스 프로브 2026-08-14, 2회 반복 완전 일치)** —
  두 세션의 구현을 병합하며 4조합을 실측한 결과:
  | 주입 \ 제출 | jQuery.Event trigger | 네이티브 untrusted KeyboardEvent |
  | --- | --- | --- |
  | **실타이핑**(`steps._type_keyword`) | ✅ 4건 | ✅ 4건 |
  | JS 세터(`SET_KEYWORD_JS`) | ❌ 팝업 소멸 | ❌ 팝업 소멸 |
  즉 `SET_KEYWORD_JS` 로 값을 넣으면 **제출 방식과 무관하게** 팝업이 죽는다. 확정 구현은
  **실타이핑 주입 + SUBMIT_KEYWORD_JS 제출**이다(제출은 둘 다 되지만, 리셋 마커 0건이 관측된
  jQuery 쪽을 채택). ⛔ 검색어 주입을 JS 세터로 되돌리지 말 것.
  ⚠ **검색어 '#' 금지(별도 실패 모드 — 진단 프로브 2026-08-14)**: 제출 방식을 고쳐도
  `'MISC-ESR3 #2'`('#' 포함)·`'2013'`(PJT_NO 숫자) 검색은 **팝업이 즉사**하고,
  `'CX85-137'`(1건)·`'MISC-ESR3'`(4건, 2013 포함)는 생존한다. 규칙: 검색어는 콤마·'#' 앞
  토큰으로 정화(`params.sanitize_keyword`, 프론트 `deriveKeyword` 미러)하고, 넓어진 결과에서
  **PJT_NO 로 정확 선택**한다(번호가 없으면 단일 결과일 때만 자동 특정 — 추측 금지).
  ⚠ 현재 구현은 검색어 주입을 **실타이핑**(`steps._type_keyword` — 오픈 직후 #keyword
  포커스+전체선택 상태를 이용, 값 검증 실패 시 트리플클릭 폴백)으로 한다.
- **D11 프리필 불신(프로브 실측)**: 프로젝트 필드는 세션/계정에 이전 값이 남아 자동 프리필된다
  (진입 즉시 'CX85-137, 12CH PROCESS' 코드 2297 채워짐). 자동화는 프리필을 신뢰하지 말고
  **항상 명시적으로 재검색·선택**한다.

## ⚠ 저장·결재 주의

- 저장(D4·D5)·거래처 적용(D8)·납기 확정+저장(D9)은 전부 게이트 뒤. 스크립트 안정화 중 게이트 닫음.
- **상신(D7)은 승인됐지만 별도 게이트다(2026-08-25).** 저장 게이트와 **분리된 플래그**
  (`allow_submit` 상당)로 잠그고, 앞 구간이 전부 통과하기 전에는 켜지 않는다. 프로브·중간 테스트
  에서는 상신하지 않는다. 켜기 전 **회수 경로 실측이 선행 조건**(eap-approval-cancel 선례).
- **개방 순서(사용자 확정 2026-08-25)**: ① 저장 → ② 조회·선택·결재창 진입(상신 직전 정지) →
  전 구간 PASS 확인 → **상신 개방** → ③ 진행. 상신 없이 ③ 을 볼 수 있는지(결재 전 구매요청이
  `구매요청` 팝업에 뜨는지)는 미확인이라, 안 뜨면 이 순서가 유일한 경로다.
- e2e 반복 테스트 전 **구매요청 삭제 수단**(F6 상당) 존재를 먼저 실측한다. 삭제 수단이 없으면
  비가역 동사 직전에서 멈추는 사이클로 설계한다(리포 안전 경계).

## 정적 증거(프로브 전 확보분)

- 화면 ① 스크린샷(2026-08-12): 필드 — 프로젝트(돋보기 피커), 구매요청일, 상위품목, 구매그룹,
  구매조직, 구매요청번호/이동요청번호, 통화(KRW), 구매사유. 필터 행 — 이동출고저장위치·
  이동입고저장위치·구매요청저장위치·거래처·납기예정일·비용센터(각각 [적용] 버튼). 체크 —
  구매요청/이동요청. 그리드 컬럼 — No/레벨순번/상위품목코드/품목코드/품목명/규격/단위/BOM수량/
  기구매요청수량/기이동요청수량/요청잔량/이동요청수량.
- 그리드 엑셀 내보내기(`PUOPRQ00200_X20616_1000_3200_mstGrid.xlsx`, 42열 355행): 레벨 구조
  1(프로젝트)/2(장비)/3(모듈·SET)/4(부품), 거래처 컬럼에 의사 거래처(가공품·판금품)+실거래처,
  WBS명(`PO-2026-07-4136`), 구매대상 Y/N. → 프론트 픽스처 `src/lib/data/purchase-order-bom.json`.

## BOM 트리그리드 실측(2026-08-13 프로브 — ✅)

- 구현체는 **`.dews-ui-treegrid`**(RealGrid **TreeGrid**) — 기존 nbkit 이 다루던 `.dews-ui-grid`
  (플랫)와 다른 신규 위젯 타입. 접근: `$('.dews-ui-treegrid').data('dewsControl')._grid`.
- 데이터 컬럼 42개 — 엑셀 내보내기 헤더와 일치(fieldName 상세는 프로브 아티팩트
  `backend/e2e/artifacts/purchase_order_bom_grid_results.json`).
- **레벨 판정**: `ds.getLevel(itemIndex)` — 0-idx **0=루트/1=프로젝트(라벨 행)/2=장비/3=SET/
  4=부품** (levelmap 프로브 재실측 2026-08-13, `/tmp/po-live-refix/levelmap-probe.json`).
  **부모 연결**: `ds.getParent(itemIndex)` 또는 필드 `UP_PJTBOM_UID_NO`.
- ⚠ **인덱스 공간 함정(2026-08-13 blocker 실측)**: 트리그리드에서 `grid.getValue(i, f)` 는
  `ds.getLevel(i)` 보다 **한 행 앞선 데이터**를 돌려준다 — 이 조합으로 읽으면 모듈이 '첫
  부품'으로, 마지막 부품이 '다음 SET 헤더'로 밀린다(스모크 산출물 대조로 발견). 필드는
  반드시 **`ds.getValue(i, f)`**(= `ds.getJsonRow(i)`) 로 읽어야 레벨과 같은 행이다.
  이전 '0=프로젝트/1=장비/2=구조행(접힘)' 매핑은 이 시프트가 만든 오독이었다.
- **전량 읽기**: `ds.getJsonRows(0, N-1)`(범위형)는 트리그리드에서 **null 반환**(플랫 전용 API —
  실측). `for i in 1..rowCount: ds.getLevel(i) + ds.getValue(i, field)` 루프가 유일한 경로
  (**1..count 포함** — 0 은 숨은 루트, 마지막 데이터 행은 i==count. boundary 프로브 실측).
- **체크 API**: `getCheckedItems()` / `getCheckedRows()` / `checkChildren()`(부모 체크 → 자식
  전파, D9 '하위 전체 선택'과 부합). 단일 행 체크 **세터는 미확정** — 쓰기 프로브에서 실클릭으로.

## 화면 ① 쓰기 경로 실측(2026-08-26 드라이런 — ✅, 저장 0건)

`backend/e2e/purchase_order_screen1_dryrun_probe.py` — 사용자 지시("저장버튼을 제외하고 진행")로
**저장 직전까지 전 구간을 조작**하고 저장(F7/`button.main-button.save`)은 좌표만 기록했다.
프로젝트 2285(`GY03-019`), 5회 반복 끝에 전 항목 확정. **잔존 데이터 0.**

### ⚠⚠ 인덱스 공간 함정 2 — 체크 API는 **아이템 인덱스** 공간이다
리포에 이미 기록된 `grid.getValue` vs `ds.getValue`(한 행 앞섬) 함정과 **같은 계열의 더 큰 함정**.

| 공간 | API |
|---|---|
| **데이터 행**(우리 리더가 쓰는 공간) | `ds.getLevel` · `ds.getValue` · **`checkRow` · `checkRows` · `getCheckedRows`** |
| 아이템 인덱스 | `checkItem` · `checkItems` · `getCheckedItems` |
| 변환 | `getItemIndex(dataRow)` · `getDataRow(itemIndex)` · `getRowsOfItems` · `getItemsOfRows` |

실측 피해: `ds` 로 계산한 자손 행 10개(4~13)를 `checkItem` 에 넘기자 **99행**이 체크됐다 —
다음 SET(행 14)과 그 하위 전체가 딸려왔다. `checkItems` 배열 호출도, `checkItem` 단건 루프도
동일하게 99행. **API 종류가 아니라 인덱스 공간의 문제**였다.
⛔ **`checkItem`/`getCheckedItems` 를 ds 인덱스와 섞지 말 것.** 리더가 `ds` 공간이므로
**`checkRow`/`getCheckedRows` 로 통일**한다.
→ 저장까지 갔다면 의도한 발주단위 대신 **다음 발주단위까지 섞인 구매요청**이 생성됐을 것이다.
   화면상 그럴듯해 보여 사후 추적이 매우 어려운 오염이라, 저장 게이트를 닫아둔 덕에 잡혔다.

### 확정된 조작 프리미티브

| 항목 | 확정 방법 | 성공 판정 |
|---|---|---|
| **전체 선택** | `grid.checkAll(true)` | 0 → 164행, `onItemAllChecked` 1회 발화 |
| **SET 행 선택** | **`grid.checkRow(<ds행>, true)`** | 0 → **11행**(SET 1 + 자손 10) — **자손 자동 전파** |
| 자손 전파 | **불필요** — `checkRow` 가 이미 전파한다. `checkChildren` 은 인자 3종(`[i,t,t]`/`[i,t]`/`[i]`) 전부 무변화(11→11) | 기대 집합과 `getCheckedRows()` **정확 일치** 검증 |
| **이동출고저장위치** | 코드피커 `n_public_sl_cd` 를 **document 스코프**로 열어 '공용자재' 검색·선택 → `[적용]` **`b_public_sl_cd`** | 리프 행에 `OUT_MV_SL_CD='1100'`, `OUT_MV_SL_NM='공용자재'` |
| **이동입고저장위치** | 코드피커 `n_mv_sl_cd` → '프로젝트' → `[적용]` **`b_mv_sl_cd`** | 리프 행에 `IN_MV_SL_CD='1000'`, `IN_MV_SL_NM='프로젝트'` |
| **납기예정일** | `#i_bfdedt_dt` 에 `YYYY-MM-DD` 입력 → `[적용]` **`btn_bfdedt_dt`** | 체크된 행에 `BFDEDT_DT` 세팅 |
| **구매사유** | `#i_rmk_dc` 텍스트 입력 — **`[적용]` 버튼 없음** | 필드 값 재확인 |
| 저장 버튼 | `button.main-button.save` @ (1863, 72), disabled=false | ⛔ 드라이런에서 클릭 안 함 |

⚠ **`[적용]` 버튼 6개는 rowText 로 구분할 수 없다** — 같은 컨테이너를 공유해 `closest('tr'/'li'/'div')`
의 텍스트가 6개 모두 동일하다(라벨 키워드 매칭은 항상 첫 번째 = 비용센터를 집는다).
**반드시 버튼 id 로 클릭**한다:

| 필터 | 입력 필드 | `[적용]` 버튼 | x |
|---|---|---|---|
| 이동출고저장위치 | `n_public_sl_cd` | `b_public_sl_cd` | 594 |
| 이동입고저장위치 | `n_mv_sl_cd` | `b_mv_sl_cd` | 891 |
| 구매요청저장위치 | `i_sl_cd` | `btn_sl_cd` | 1187 |
| 거래처 | `i_partner_cd` | `btn_partner_cd` | 1428 |
| 납기예정일 | `i_bfdedt_dt` | `btn_bfdedt_dt` | 1632 |
| 비용센터 | `i_cc_cd` | `btn_cc_cd` | 1884 |

⚠ `codepicker._open_picker` 는 card_collect 모달 전용이라 이 화면에도 안 먹는다 —
**오픈만 document 스코프로 직접 작성**하고 검색/선택/적용/닫힘은 `js_lib.PICKER_*` 재사용(화면 ② 선례와 동일).

### 다이얼로그 2종 — 문구·조건 확정
| 시점 | 문구 | 조건 | 처리 |
|---|---|---|---|
| 조회 | **`저장하지 않은 데이터가 있습니다. 조회를 계속하시겠습니까?`** `[예][아니요]` | **미저장 변경이 있을 때만** | `[예]` |
| 저장 | `저장하시겠습니까?` `[예][아니요]` | 저장 클릭 시 | `[예]` (드라이런에선 미도달) |

✅ **가설 적중** — 1·2차에서 안 뜬 것은 `[적용]` 이 실패해 진짜 변경이 없었기 때문이고, 저장위치가
실제로 그리드에 꽂히자마자 떴다. 사용자가 말한 '초기화 메시지'가 이것이다.
⚠ **조회 재시도 규율(필수)** — 헤드리스 압축 실행(`delay_scale=0.4`)에서는 `[예]` 후 첫 조회가
그리드에 반영되지 않고 **2회차에 반영**됐다(3·4·5차 재현: try1 164행 → try2 664행).
✅ **헤디드 실시간 실행(`delay_scale=1.0`)에서는 1회로 반영** — 즉 '조회 2회'는 ERP 의 요구사항이
아니라 **다이얼로그 처리와 조회 발사 사이 여유가 없어 생긴 타이밍 아티팩트**다.
그러므로 횟수를 고정하지 말고 **기대 행수에 도달할 때까지 재시도**하고, 끝내 실패하면 조용히
넘어가지 말고 **명시적으로 실패**시킨다 — 2차에서 이 누락 때문에 이후 단계 전부가 잘못된
데이터셋(이동요청 164행) 위에서 돌았다.

### 미해소
- **캔버스 좌표 실클릭**: 체크바 좌표 계산(캔버스 `263,287,1642×888` + `rowHeight 32` +
  `checkBar.width 20`)으로 x 3종 × y 20~140px 스캔했으나 **전 구간 미적중**. `getCellRect` 없음.
  → **API(`checkRow`) 경로 채택**. 앱 핸들러(`onItemChecked`/`onItemAllChecked`)가 실제로 발화하므로
  `setValue` 우회와는 성격이 다르다. 단 **저장이 이 체크를 실제로 반영하는지는 저장 게이트를
  열어야 확정**된다.

## 화면 ②③ 랜딩 실측(2026-08-25 프로브 — ✅, 부작용 0)

`backend/e2e/purchase_order_screen23_dump_probe.py` — ②③ 진입이 **재현 가능한 코드로 보존된 첫 사례**.
둘 다 `navigate_menu(grids_required=2)` 1차 시도 성공(폴백 불요).

### 화면 ② 구매요청처리 `/PU/PUOPRQ00300_X20616` — `.dews-ui-grid` **5개**
마스터 1 + 탭 4가 **항상 DOM 공존**(탭 전환은 CSS show/hide, 각 탭 그리드는 이미 로드됨).

| 필드 | id | 종류 | 필수 |
|---|---|---|---|
| 공장 | `#s_plant_cd` | 코드피커 | ✅ `dews-state-required` |
| 구매요청일 | `#s_purreq_dt`(`_startinput`/`_endinput`) | periodpicker | ✅ |
| 요청번호 | `#s_no_purreq` | 텍스트 | — |

- 툴바 버튼(이 4개가 전부): `#btn_requestCancel`(요청취소) · `#btn_requestClose`(요청마감) ·
  `#btn_closeCancel`(마감취소) · `#btn_file`(첨부, disabled)
- 우측 아이콘 바(class 식별 — title/aria **공란**): `main-button information` · `localize`(disabled) ·
  **`approval`** · **`etn-approval`** · `lookup` · `delete` · `print` · `favorite`
  → **결재 진입 후보 = `button.main-button.approval`(x≈1715,y≈56) / `button.main-button.etn-approval`
  (x≈1748,y≈56)**. **랜딩부터 활성**이며 조회·행선택으로 새로 나타나지 않는다(조회 전후 diff에서 신규
  버튼 0). 둘 중 어느 쪽이 셀프결재인지 — 검증: ❓ (라벨이 없어 실클릭 전에는 확정 불가)
- 마스터 컬럼: `PURREQ_NO, PURREQ_DT, PURREQ_TP_NM, PURDOC_TP_NM, PURGRP_NM, KOR_NM,`
  **`ATHZ_ST_NM`(결재상태)**`, `**`GWDOCU_NO`(결재상신코드)**`, RMK_DC, FILE_DC`
- 탭 그리드 컬럼:
  - 품목 `ITEM_CD, ITEM_NM, ITEM_SPEC_DC, PURREQ_UNIT_CD, PURREQ_QT, EXCH_CD, BFDEDT_DT,
    PURREQ_UM, PURREQ_AMT, VAT_AMT, TOTAL_AMT, REQN_LINE_ST_NM, REQN_CNCL_YN, REQN_CNCL_DT,
    REQN_CLOSE_DT, RMK_DC, MDL_NO`
  - 거래처 `PARTNER_CD, REQN_PARTNER_NM, SL_NM, PO_PRICE_INFO_NO, PO_PRICE_INFO_SQ, UM_COND_NM`
  - 계정 `PURREQ_QT, PURACCT_DIST_RT, BFDEDT_DT, ACCT_CD, ACCT_NM, BUDGET_NM, BGACCT_NM,
    BIZPLAN_NM, WBS_NO, WBS_NM, PJT_NO, PJT_NM, SO_NO, CC_NM, RMK_DC`
  - 발주 `PURDOC_NO, PURDOC_SQ, PURDVYSCH_SQ, PUR_PO_QT, PURDOC_CTGRY_NM, PO_LINE_ST_NM,
    EXCH_CD, EXRT_RT, PO_UNIT_CD, PUR_TRAN_UM, PUR_TRAN_AMT, PO_CNCL_YN`
- ⚠ **코드피커 재사용 함정**: `nbkit.omnisol.codepicker._open_picker` 는 **card_collect 전용**
  (`.k-window` 타이틀 "법인카드" 스코프)이라 이 화면의 최상위 폼 필드에는 **안 먹는다**(버튼 미발견
  6초 소진). **오픈만 document 스코프로 새로 작성**하고 검색/선택/적용/닫힘은 `js_lib.PICKER_*`
  (document 전역 스캔)를 그대로 재사용하면 성공한다 — 실측 확정.
- 조회 실측: 공장=나인벨 + F2 → 62행, **전부 `결재상태=종결`**. 미결(`저장`) 문서가 없어 결재 버튼의
  **동적 노출 여부는 미확인** — 화면① 저장으로 미결 문서가 생기면 자연히 풀린다.

### 화면 ③ 구매발주일괄입력 `/PU/PUOORD02000_X20616` — `.dews-ui-grid` **4개**
마스터 + 디테일 + 숨은 보조 2개(WBS 소그리드 7컬럼, 발주상세/납기스케줄 30컬럼).

| 필드 | id | 종류 | 진입 시 값 |
|---|---|---|---|
| 공장 | `#s_plant_cd` | 코드피커 | 1000/나인벨 |
| 구매발주일 | `#s_pur_po_dt` | 날짜 텍스트 | 오늘 |
| **구매발주유형** | **`#s_po_tp_cd`** | 코드피커 | **공란 — 유일한 입력 대상(원재료)** |
| 구매그룹 | `#s_purgrp_cd` | 코드피커 | 1000/나인벨 |
| 구매조직 | `#s_purorg_cd` | 코드피커 | 1000/나인벨 |
| 통화 / 부가세포함여부 | `<select>` (id 미포착 — 보완 필요) | 네이티브 select | KRW·환율1(readonly) / 부가세별도 |
| 세무구분 | `#s_taxafs_cd` | 코드피커 | 21/과세매입 |

- **`구매요청` 버튼 = `#btn_req`** (x≈1841,y≈109)
- **납기예정일 입력 `#BFDEDT_DT` + 적용 버튼 `#btnApplyDT`** (x≈1884,y≈473)
- 툴바 아이콘 5개뿐: `information` · `localize`(disabled) · **`add`(＋)** · **`save`(💾)** · `favorite`
  (조회·삭제·인쇄·결재 아이콘 없음 — 입력 화면이라 정합적)
- 마스터 컬럼: `PLANT_NM, PURDOC_NO, PURORG_NM, PURGRP_NM, PUR_PO_DT, WBS_NM, PJT_NM,`
  **`PARTNER_NM`(거래처명)**`, PO_TP_NM, TERPAY_CD, `**`TERPAY_NM`(결제조건)**`, EXCH_CD, EXRT_RT,
  VAT_INCL_YN, `**`RMK_DC`(비고)**
- 디테일 컬럼: `PURREQ_NO, PURREQ_SQ, PUR_LINE_TP, PURACCT_CTGRY_CD, ITEM_CD, ITEM_NM,
  ITEM_SPEC_DC, ITEM_MTRQLT_DC, ITEM_GRP_NM, PO_UNIT_CD, PUR_PO_QT, PUR_TRAN_UM, PUR_TRAN_AMT,
  PO_UM, PO_AMT, VAT_AMT, TAXAFS_CD_NM, SL_NM, `**`BFDEDT_DT`(납기예정일)**`, WBS_NO, PJT_NM,
  PROD_NO, PROD_SQ, CC_NM, RCPT_PATH_FG, RMK_DC, CSF_CD, TERM_SQ, NUME_TRTE_VR, DNOM_TRTE_VR`

### 메뉴 트리 — `구매발주처리[나인벨]` 실재 확인
화면코드 **`PUOORD00200_X20616`**, 딥링크 **`/PU/PUOORD00200_X20616`**. `구매발주관리`(menu-PUOORD)
그룹의 마지막 항목. 화면③(`PUOORD02000_X20616`)과 **다른 화면**이다(`00200` vs `02000`).
사용자는 이 플로우에서 쓰지 않는다고 했으므로 **범위 밖**이지만, 존재 사실은 기록해 둔다.

## 화면 ③ 조작 실측(2026-08-28 헤디드 프로브 run2 — ✅, 저장 0회·잔존 0)

`backend/e2e/purchase_order_screen3_ops_probe.py`(아티팩트 `e2e/artifacts/po_screen3_ops_*`). 대상 = ETRI-001 의
셀프결재 종결 PRQ2026080694~0702. 사용자 시연 영상(`발주과정 화면.mp4` 7:55~9:19)과 정합.
D8 의 '✅(시연)' 은 전부 **✅(라이브)** 로 격상, "하단 적용 비가역 여부 ❓" 는 **비가역 아님**으로 정정.

| 항목 | 확정값 |
|---|---|
| 그리드 인덱스 | `.dews-ui-grid[0]`=마스터(`PARTNER_NM,TERPAY_NM,RMK_DC,WBS_NM,PJT_NM,PURDOC_NO…`) / `[1]`=디테일(`PURREQ_NO,ITEM_CD,BFDEDT_DT…`) / `[2]` WBS 소그리드 / `[3]` 발주상세(숨김) |
| 구매발주유형 | `steps_write.pick_code_document(page,"s_po_tp_cd","원재료")` → 코드 1000/표시 원재료. 코드 `1000` 직접 타이핑도 동일(영상) |
| 팝업 오픈 | `#btn_req` → `.k-window` 제목 **구매요청**, **열리면서 자동 조회**(조회 버튼 없음). 요청유형 `s_purreq_tp` 원재료 프리필, 요청일 `s_purreq_dt_startinput/_endinput` 기본 **당월 1일~말일** |
| 팝업 필드 id | 구매유형 `s_purdoc_tp`(kendo dropdown)·품목 `s_item_cd`·저장위치 `s_sl_cd`·품목주거래처 `s_principalpartn_cd`·**변경거래처 `s_chg_partner_cd`**·**구매요청번호 `s_purreq_no`**·WBS요소 `s_wbs_no`(multicodepicker, 미사용) |
| 팝업 버튼 | 행 변경거래처 적용 **`#btn_apply`** / 하단 확정 **`button.confirm.ok`**("적용") / 닫기 `button.confirm.cancel` |
| PRQ 특정 | `#s_purreq_no` 에 PRQ 입력 + **trusted Enter** → 그 PRQ 라인만(647→84행). 팝업 소멸 없음(프로젝트 도움창과 다름) |
| 거래처 변경 | 팝업 그리드 `checkRow`(대상 행) → `pick_code_document(page,"s_chg_partner_cd","해룡"/"알파테크")` → `#btn_apply` → 스낵바 **"적용되었습니다."** + 체크 행만 `CHG_PARTNER_NM/CHG_PARTNER_CD`(30209/10061). 품목거래처명 fieldName = **`PRINCIPALPARTN_NM`**(가공품/판금품/실거래처명) |
| 하단 적용 | 대상 행 체크 → `button.confirm.ok` → 다이얼로그 **"적용하시겠습니까?" [예]** → 팝업 닫힘 → **마스터 1행 = 거래처 1종**, `RMK_DC` 프리필 = 구매사유(D6), `TERPAY_NM` 자동 |
| 마스터→디테일 | 마스터 행 **실 마우스 클릭**(bbox 헤더 ~30px + 행 ~32px)으로 디테일 재조회(ArrowDown 불필요) |
| 납기 | 디테일 `checkAll(true)` → `#BFDEDT_DT`=`YYYY-MM-DD` + `#btnApplyDT` → `BFDEDT_DT` JS Date(UTC, KST 자정) 반영 |
| 비고 | `setCurrent({itemIndex,fieldName:'RMK_DC'})+showEditor()` → 오버레이 `#PUOORD02000_X20616_1000_3200_mstGrid_line`(실 input). 타이핑→Tab→`ds.getValue` 커밋 확인은 ❓(1스텝 잔여) |
| 폐기 | 딥링크 재진입만으로 초기화, 경고 다이얼로그 없음, 마스터 0행(2회 확인) |
| ✅ 저장(에이전트 e2e 2026-08-28) | 💾 → 마스터 `PURDOC_NO` 가 **`PUR2026082149` 형태**로 전 행 발급 = 성공 신호(PRQ2026080694 → 거래처 5행 → PUR…2149~2153). 삭제 아이콘 없음(비가역) → 사용자 승인 (a) 로 confirm HITL 뒤 실행 |
| ✅ 비고 커밋 | 에디터 오픈 후 **로케이터 `fill` + Enter** 만 커밋됨(Ctrl/Meta+A 타이핑+Tab 은 미커밋). `steps_screen3.set_master_note` 폴백 체인(fill→type→setValue) |
| ✅ 마스터 행 선택 | 좌표 클릭은 y 를 바꿔도 마지막 행이 잡힘 → **클릭 후 방향키로 `getCurrent()` 가 idx 될 때까지 이동**(디테일 재조회 발화) |
| ✅ 과거 납기 | 발주일 이전 날짜는 [적용]이 조용히 무시됨 → 에이전트는 경고 로그 후 구매요청 납기 유지(`due_before_today`) |
| ✅ 코드피커 선택 | 검색 직후 첫 행 맹선택 금지(알파테크 검색이 'ACM Research' 선택) → `PICKER_FIND_ROW_JS` 로 검색어 포함 행 선택 |

## 스텝 계획 (Phase A 구현분 + Phase B 예정분)

| 순서 | 스텝 | 내용 | 검증 | 비가역 |
|---|---|---|---|---|
순서 변경(2026-08-25): 이동요청 저장을 **계획서 승인 뒤**로 옮겨, 비가역 저장이 전부 승인 뒤에 온다.

| 순서 | 스텝 | 화면 | 내용 | 검증 | 비가역 |
|---|---|---|---|---|---|
| 1 | login | — | 공용 로그인(nbkit) | ✅ 기존 | |
| 2 | user_type | — | **SCM-구매** 전환 — `ensure_user_type(page,"SCM")` 무수정 | ✅ | |
| 3 | menu_nav | ① | `/PU/PUOPRQ00200_X20616` 딥링크 — `navigate_menu()` 무수정 | ✅ | |
| 4 | project_set | ① | 도움창 검색(팝업당 1회·재오픈 재시도)→선택→적용→반영 확인→조회 | ✅ | |
| 5 | read_bom | ① | 이동요청 해제→조회→트리그리드 전량 읽기→계획서 BOM 조립 | ✅ | |
| 6 | plan | — | 계획서 HITL(`planner` kind) — 발주단위·사유·납기·거래처 확정 | ✅ | |
| 7 | move_req | ① | D4 이동요청 일괄 저장(이동요청만→조회→`checkAll`→저장위치 `[적용]`→저장) | ✅조작/❓저장 | ⚠ |
| 8 | unit_loop | ① | D5 발주단위 반복(재조회[예]→`checkRow`→사유·납기 `[적용]`→저장) | ✅조작/❓저장 | ⚠ |
| 9 | req_process | ② | D7 조회(공장·`PRQ…`)→행 선택→결재창 진입 — **상신 직전 정지** | ✅진입/❓조작 | |
| 10 | submit | ② | D7 **상신** — `allow_submit` 게이트. 회수 경로 실측 후 개방 | ❓ | ⚠⚠ |
| 11 | po_batch | ③ | D8 `#s_po_tp_cd`→`#btn_req` 팝업→WBS 요소→거래처 그룹 적용→적용 | ✅진입/❓조작 | ⚠ |
| 12 | po_close | ③ | D9 거래처 행별 비고 · `#BFDEDT_DT`+`#btnApplyDT` → **💾 저장** | ✅진입/❓조작 | ⚠ |

스텝 9·11·12 는 **진입·랜딩 구조·셀렉터가 프로브로 확정**(위 '화면 ②③ 랜딩 실측' 절)이고, 실제
**조작(입력·선택·적용·저장)은 미실측**이다. 스텝 10(상신)은 별도 게이트로 잠겨 있다.

## HITL 통합 설계(2026-08-13 인프라 탐색 — hitl-map)

D1(실행 중 HITL)의 구현 레시피. 선례 = `grid` kind(card_collect)가 밟은 6군데.

- **개입 2회**: ① 프로젝트 선택 — 기존 `search`/`select` 계열 재사용(query 왕복 루프 =
  card_collect `collect.py:326-392` 패턴, layoutLevel 'split' 유지 — 라이브 화면 보임).
  ② 계획서 — **신규 kind `planner`**, layoutLevel 'full'(그리드 개입과 동일).
- **백엔드 변경점**: `runs.py` `HitlDecision` 에 `plan` 필드 + **:301-310 payload dict 에도
  동시 추가**(누락 시 조용히 버려짐 — 2026-07-05 grid 회귀 선례). `hitl.py` 는 무변경
  (`**extra` 가 확장점). 노드는 `open_hitl_channel` + 매 턴 프레임 전체 재방출.
  픽스처는 workflow_id·steps 채우고 HITL 두 스텝에 `intervention: True`.
- **프론트 변경점**: `types.ts`(kind 유니온·프레임 extras·제출 payload·액션 4곳),
  `use-live-run.ts` `sendPlan`(sendRows 옆, 낙관적 clearHitl), `live-side-panel.tsx:94-113`
  kind 분기, `agent-detail-client.tsx:173` 'full' 조건에 planner 추가,
  신규 `LivePlannerCard`(데모 `simulation/purchase-order/*` 조각 재사용 — 데모 패널과 실
  개입은 agentId 가 달라 공존).
- **운영 제약(실측)**: 응답 스키마는 폐쇄형(7키) — plan 제출 필드에 `max_length` 명시(rows=500
  선례). hitl 프레임은 DB 미저장·세션 버퍼 상주(400행 ≈ 150-300KB, 재방출 비용 감수).
  HITL 대기 기본 600초/1회(계획서 대기는 `timeout_s` 상향 — 대기 중 런 예산은 자동 정지),
  타임아웃 시 런 failed(재개 없음). 탭 닫으면 즉시 취소. ⚠ 로컬 `--reload` 는 개입 중
  `.py` 수정 시 큐가 날아간다. 백/프론트 **동시 배포 필수**(백엔드 선배포 = 제출 버튼 없는 카드).

## 남은 작업(라이브 ERP 필요 — flow-buildout 표준절차)

- [x] **Phase A 구현 완료**(2026-08-13 스모크 SUCCESS) — login→user_type→menu_nav→pick_project→
      read_bom→plan(HITL)→report. 쓰기 0.
- [x] 화면 ①②③ 전 구간 경로 확정 — 사용자 시연(2026-08-25). D3·D4·D5·D7·D8·D9 갱신 완료.
- [x] **읽기 프로브 완료**(2026-08-25): 2285 발주 가능 판정 ✅ / 화면 ②③ 랜딩 DOM 전량 덤프 ✅ /
      `구매발주처리[나인벨]` 실재 확인 ✅. 미해소 2건은 아래로 이월.
- [x] **화면① 드라이런 완료**(2026-08-26, 5회 반복, 저장 0건): ⭐4개 항목 전부 확정 +
      **인덱스 공간 함정 2** 발견 + 초기화 다이얼로그 조건 해소. '화면 ① 쓰기 경로 실측' 절 참조.
- [ ] **쓰기 프로브(저장 게이트 개방) — 다음 차례**: 조작 경로는 전부 확정됐으므로 남은 미지는
      **저장이 `checkRow` 체크를 실제로 반영하는가** 하나다(캔버스 실클릭 경로가 없어 API 체크가
      영속되는지는 저장해 봐야 안다). D4 이동요청 저장 1회 → 토스트·`IRQ…` 확인 →
      **되돌리기 수단 확인**(화면 ② `#btn_requestCancel`) 전까지는 D5 로 넘어가지 않는다.
- [ ] 화면② 결재 아이콘 2개(`approval` / `etn-approval`) 중 어느 것이 셀프결재인지 —
      라벨이 없어 hover 툴팁 또는 실클릭으로만 확정 가능
- [ ] **상신 개방 전 선행**: `eap-approval-cancel` 이 구매요청 결재 문서도 회수하는지 실측.
      불가 시 상신 직전 정지 설계로 전환
- [ ] 화면 ②③ MenuSchema 등록 — `grids_expected` 실측치 확보됨(**② 5개 / ③ 4개**,
      둘 다 `grids_required=2` 로 1차 진입 성공). 도착 앵커·필드 셀렉터는 위 전용 절에 있음.
      `selectors.MODULE_PU` 상수는 여전히 없음
- [ ] `nbkit/OMNISOL_NOTES.md` §9 딥링크 표에 PU 3종 반영(현재 IM·FI 만 실려 문서-코드 불일치)
- [ ] 미확인: **결재 전(결재상태=저장) 구매요청이 화면 ③ `구매요청` 팝업에 뜨는지**
      — 안 뜨면 ③ 테스트는 상신 이후에만 가능

## 검증 로그

- 2026-08-26 **화면① 쓰기 드라이런**(`purchase_order_screen1_dryrun_probe.py`, 5회 반복,
  **저장 0건·잔존 0**). ⭐확정: `checkAll(true)` 전체선택 / **`checkRow(ds행, true)` = SET+자손 자동
  전파** / 저장위치 코드피커·`[적용]` 버튼 id·반영 컬럼(`OUT_MV_SL_*`/`IN_MV_SL_*`) / 납기
  `#i_bfdedt_dt`+`btn_bfdedt_dt`→`BFDEDT_DT` / 구매사유 `#i_rmk_dc`(적용버튼 없음) /
  다이얼로그 2종 문구·조건 + **조회 2회** 규율.
  ⚠⚠ **인덱스 공간 함정 2 발견** — `checkItem`/`getCheckedItems`(아이템 공간)에 ds 행 번호를
  넘겨 자손 10개 요청이 **99행 체크**로 번졌다. `checkRow`/`getCheckedRows`(행 공간)로 통일해 해소,
  기대 집합 **정확 일치** 검증을 상시 걸었다. 저장했다면 발주단위가 섞인 구매요청이 생겼을 건이다.
  ❌ 미해소: 캔버스 좌표 실클릭 전 구간 미적중(`getCellRect` 없음) → API 경로 채택.
  ⚠ 실패에서 배운 규율 2건: `[적용]` 6개는 rowText 로 구분 불가(**버튼 id 필수**),
  조회 미반영을 조용히 넘기면 이후 단계가 통째로 잘못된 데이터셋 위에서 돈다(**명시적 실패** 필요).
- 2026-08-25 **읽기 프로브 2종**(`purchase_order_2285_probe.py`, `purchase_order_screen23_dump_probe.py`,
  부작용 0 — 저장·구매요청·요청취소·상신·결재 클릭 0건).
  ✅ 2285(`GY03-019`) 발주 가능 판정(구매요청만 664행·리프 633·SET 28 전부 유효) /
  화면 ②③ **랜딩 DOM 전량 덤프**(필드 id·툴바 버튼 id·아이콘 class·그리드 컬럼 — 위 전용 절 참조) /
  결재 진입 후보 `button.main-button.approval`·`.etn-approval` 특정 /
  `구매발주처리[나인벨]` = `PUOORD00200_X20616` 실재 확인.
  ❌ **초기화 다이얼로그 재현 실패(4/4, DOM+네이티브 이중 계측)** — 미저장 변경 가설로 이월.
  ❌ 화면② 결재 버튼 동적 노출 여부 — 조회 62행이 전부 `종결`이라 **데이터 없음**으로 미확정.
  ⚠ 신규 함정: `codepicker._open_picker` 는 card_collect 모달 전용이라 최상위 폼에 못 쓴다
  (오픈만 document 스코프로 새로 작성 → 성공).
- 2026-08-25 **사용자 시연으로 화면 ①②③ 전 구간 경로 확정**(스크린샷 9장, 18분 녹화 발췌).
  신규 확정: 저장 성공 토스트 `자료가 정상적으로 저장되었습니다.` / 번호 자동발급(`IRQ…`·`PRQ…`) /
  다이얼로그 2종(재조회 초기화·`저장하시겠습니까?`) / SET 체크 시 하위 부품 자동 전파 /
  화면② 결재 아이콘(툴팁 "결재")·결재상태 3값(저장·진행·종결)·결재상신코드 /
  결재창 = 회계전표와 동일 EAP 팝업(`MicroModuleCode=eap`, 버튼 미리보기·보관·상신) /
  화면③ `구매요청` 팝업 전 구조 + `[적용]` 후 `변경거래처명`·`변경거래처` 채워짐(해룡 30209·
  알파테크 10061) / 화면③ 마스터 1행 = 거래처 1건, 비고 인라인 편집, 납기 `[적용]`, 💾 저장 종료.
  **정정 3건**: ① D3 구매사유 — '비움'은 진입 시 프리필 제거이고 루프에서는 발주단위 사유를 채운다,
  ② D5 다이얼로그 — '없음' 기록은 최초 1회 조회만 봤던 탓, 재조회·저장에서 각각 뜬다,
  ③ D8 `WBS 요소` — 값은 **프로젝트명**이지 `PO-…`(WBS_NM) 가 아니다.
  **사용자 결정**: 저장위치 2종 고정 / 이동요청 저장을 계획서 승인 뒤로 / 거래처는 계획서 값 /
  상신 자동화 승인(단 앞 구간 전부 통과 후 맨 마지막에 개방) / 화면③은 💾 저장으로 종료.
- 2026-08-13 Phase A 구현·검증: 그래프(login→user_type→menu_nav→pick_project[search HITL]→
  read_bom→plan[planner HITL]→report, 쓰기 0) + runs.py plan 필드 + 픽스처 승격 + 프론트
  planner 배선(LivePlannerCard — 데모 컴포넌트 파라미터화 재사용). 리뷰 py/ts 승인. 실 ERP
  API 스모크 SUCCESS(runId 5ead434882…): read_bom stale-grid 레이스를 내용 기반 대기
  (`wait_bom_filtered` — count+mvY 시그니처·연속 2폴)로 수정 후 354행·SET 15·부품 336 정합,
  plan 왕복·notice 재방출 라이브 확인. pytest 1680 passed·tsc 클린.
- 2026-08-13 읽기 프로브(`backend/e2e/purchase_order_{discover,menu_entry,bom_grid}_probe.py`,
  부작용 0): D2·D3 ✅(SCM-구매 라벨·전환, 딥링크 3종, 기본값 나인벨), D10 부분✅(프로젝트 도움창
  `#keyword`, PJT_NO/PJT_NM, CX85-137=코드 2297 1건), D4/D5 정정(체크박스 초기값 둘 다 체크 —
  해제가 액션), BOM 트리그리드 `.dews-ui-treegrid` 확정(getLevel/getParent 루프 리더, 410행
  실측, getJsonRows 는 null). 미해소: ZJ90 팝업 소멸, 단일 행 체크 세터, 체크박스 필터 연동,
  셀프 결재 버튼 위치. 아티팩트: `backend/e2e/artifacts/purchase_order_*`.
- 2026-08-13 후속 프로브(`purchase_order_{zj90_search,project_apply,checkbox_filter}_probe.py`,
  부작용 0): project_set 전체 사이클 ✅(CX85-137·ZJ90-130=PJT_NO 2261, 2/2), 팝업 불안정
  완화책(팝업당 검색 1회+재오픈 상한 2회) 채택, 체크박스 `#s_pu_chk`/`#s_move_chk`(label[for]
  직결) + MV_FG 필터 의미 수치 정합(410/354/73), '초기화: 예' 다이얼로그 미발생 정정,
  BOM 5레벨 정정(SET=getLevel 3, 발주단위 선택 대상). 미해소: 팝업 소멸 근본 원인(완화책으로
  대체), 이동요청만 시나리오 구조행 17 세부 구성(영향 적음).
- 2026-08-13 트리그리드 리더 blocker 수정·재검증: ① 인덱스 시프트(`grid.getValue` 가
  `ds.getLevel` 보다 한 행 앞섬 — 모듈이 첫 부품으로, 마지막 부품이 다음 SET 헤더로 밀림) →
  `ds.getValue` 단일 인덱스 공간으로 교체, ② ds 경계(0=숨은 루트, 데이터 1..count — 0-베이스
  루프가 마지막 리프 유실, '리프 336' 오독의 원인) → 루프 1..count 포함으로 수정, ③ wbs 픽
  레벨 2+ 한정(프로젝트 라벨 행 WBS_NM=프로젝트명 오염 차단), ④ `set_checkbox` 클릭 유실
  재시도(상한 3, F2 직후 1회 실측). 근거 프로브: idx/boundary(`/tmp/po-live-refix/*.json`,
  부작용 0). 재스모크 SUCCESS(runId f1956abb…, 53.8s): 모듈 15개 = `CX85-137-PROCESS-001..015`
  정확 일치, SET 헤더 parts 혼입 0, 마지막 모듈 parts 2(Z-ROBOT L/R), **리프 337**(=PUR_FG Y,
  xlsx 픽스처와 모듈별 완전 일치), wbs=`PO-2026-07-4136`, plan 왕복·notice 재방출 PASS.
  회귀 방지: JS 소스 계약 트립와이어 테스트(단일 인덱스 공간·1..count 경계) 추가.
  pytest 1685 passed·tsc 클린.
- 2026-08-14 팝업 소멸 근본 원인 프로브·수정(라이브 사전선택 529ms 2연속 실패 → HITL 폴백
  오발 신고 건): ① 진단 프로브(50ms 타임라인·콘솔/네트워크/framenavigated 계측, 4/4 재현) —
  trusted Enter = `<form>` 네이티브 제출 → SPA 소프트리셋(MainContainer 재마운트)으로 팝업
  **영구** 소멸(레이스 아님·결정적), 이물 창 가설(H1)·동기 주입 가설(H2)·대기 연장 해법(H4)
  전부 반박. 자동 사전검색·창 셸(79~187ms)/입력 준비(345~449ms) 분리 신규 실측. ② 후보 검증
  프로브 — untrusted 제출 후보 중 **jQuery.Event trigger 합격**(리셋 마커 0건, 그리드 갱신
  164ms; 네이티브 untrusted KeyboardEvent 는 기능 성공했으나 마커 1건으로 배제), 이어서
  전체 사전선택 경로(검색→선택 2261→적용 211ms 닫힘→필드 반영→F2→BOM 73행 607ms) 라이브
  PASS. 수정: `js.SUBMIT_KEYWORD_JS`(untrusted 제출)·`js.POPUP_STATE_JS`(도움창 특정 준비
  판정)·시그니처 변화+연속 2폴 정착 수락·소멸 연속 2폴 디바운스, e2e
  `project_apply`/`checkbox_filter` 프로브 정렬. steps 단위테스트 5종 추가.
