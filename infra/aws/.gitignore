# Terraform
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
# 비밀값 — 커밋 금지
terraform.tfvars
*.auto.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
# .terraform.lock.hcl 은 보통 커밋합니다(프로바이더 버전 고정) — 유지.
