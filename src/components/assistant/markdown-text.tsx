import { MarkdownContent } from '@/components/ui/markdown-content';

/**
 * 어시스턴트 메시지 마크다운 렌더 — 공용 구현(`MarkdownContent`)의 별칭.
 * LiveChatCard(라이브 실행 채팅)도 동일한 공용 컴포넌트를 쓴다(디자인시스템 정리 WS6).
 */
export const MarkdownText = MarkdownContent;
